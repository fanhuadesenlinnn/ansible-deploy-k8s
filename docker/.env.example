# 复制为 docker/.env 后填写宿主机绝对路径。docker/.env 已被项目的 .gitignore 忽略。
# 私钥可以留空并改用 SSH Agent 或 ansible-playbook --ask-pass。
ANSIBLE_SSH_PRIVATE_KEY=

# 留空时，run.sh 会自动尝试宿主机的 ~/.ssh/known_hosts。
ANSIBLE_KNOWN_HOSTS=

# 可选：显式指定宿主机的 SSH Agent Socket；留空时尝试当前 SSH_AUTH_SOCK。
ANSIBLE_SSH_AGENT_SOCKET=
