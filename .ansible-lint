---
# 使用最严格的 production 规则集，让本地检查与 GitHub Actions 保持一致。
profile: production
# 离线包包含生成内容，虚拟环境包含第三方代码，都不属于项目源码检查范围。
exclude_paths:
  - files/offline-bundle/
  - .venv/
# 项目变量集中定义在 group_vars 中，不强制添加 Role 前缀；行长度交给 yamllint 统一提示。
skip_list:
  - yaml[line-length]
  - var-naming[no-role-prefix]
